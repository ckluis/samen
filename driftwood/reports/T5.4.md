# T5.4 — Crypto-shred game-day against the live Driftwood app

**This file is machine-generated by `priv/gameday/crypto_shred_gameday.exs` and is
the auditor artifact the vision doc says you show an auditor.** It is regenerated
(and re-verified) on every Driftwood CI run via `driftwood/ci.sh` step 19.

Generated: 2026-07-17T01:32:57.900823Z
KMS keystore (FileBacked, shared with the oracle subprocess): `/var/folders/_6/44y5p5453njbczf2l4nzx0_w0000gn/T//driftwood_gameday_kms.pfVZ9y`
Repo: `Driftwood.Repo` → driftwood_test (local Postgres, committed — no sandbox)

## What this proves

A REAL Driftwood driver subject — name / emails / phones / `pii_drv_cdl_number` —
is seeded so its PII lands on EVERY tier (domain rows, vault, aud_event, the
driver load-count rollup, oban job args, the audit/impersonation chain from a
reveal, and the registered `non_pii!` carve-out). Then `Samen.Erasure.shred/2`
runs, and the destruction oracle
`mix samen.verify.no_plaintext_pii --subject <uuid> --tiers all` is run exactly as
an auditor would (a separate OS process) and asserted to EXIT 0. Both rollup arms
(rebuild + suppress) are exercised; the audit chain is proven to still VERIFY
post-shred while the driver is unrecoverable; every red path flips the oracle; and
an anti-tautology sabotage is caught then reverted.

## Deploy seam (OPERATOR TODO — nothing faked)

This runs LOCALLY: local Postgres + a FileBacked KMS keystore. The vision doc's
Fly + Neon PITR + AWS KMS/S3 target is an operator TODO. The key store is EXTERNAL
to Postgres in both cases, so the load-bearing ADR-001 guarantee — a PITR restore
brings back useless ciphertext, never the destroyed key — is identical. The replica
tier is declared `--replica none` (no physical replica here); the PITR-history scan
is a documented seam (pass `pitr_repos:` from the T2.5 drill snapshots to make it a
real content scan).

## 1. Seed a real driver across EVERY tier

Subject (driver) id: `c0fb4a3c-1ea2-4b7e-8f8f-c6dae9839333`
Org id: `c8f24bbf-1312-44c4-a636-50bb5681bb98`  ·  Carrier: `37e3a2dd-3f73-4524-9621-c163e3288720`  ·  Load: `45ef90e3-58d9-4d52-ae7b-15c21e809c93`
CDL plaintext (the crown-jewel PII): `CDL-GAMEDAY-68`
Driver name: `Marisol Gameday-100`  ·  email `marisol.gameday.132@driftwood.test`  ·  phone `+1-555-712700`
Reveal request `4d1518dd-2b48-4757-8987-02fdda9b68e1` approved by `operator:auditor-demo`; revealed by `operator:desk-agent`.
Oban dispatch job id: `1`
- PASS — PRE-shred: the CDL decrypts under a distinct-party grant (`CDL-GAMEDAY-68`)
- PASS — Domain row holds a vt_ token, never plaintext CDL
- PASS — Vault: 4 active rows (name/email/phone/cdl) all decryptable pre-shred
- PASS — aud_event: 3 dispatch events keyed on the driver
- PASS — Rollup: the pre-shred load-count rollup COUNTS the driver (3 loads)
- PASS — Oban job args carry the driver id (token-only)
- PASS — Audit chain (org `__global__`) VERIFIES pre-shred (1 entries)

## 2. Crypto-shred the driver (Samen.Erasure.shred/2)

Attestation: state=`shredded` attestation_id=`file-f08f1fcd967c6914` destroyed_at=`~U[2026-07-17 01:32:58.083112Z]`
Erasure report outcome: `shredded`  ·  vault rows sealed: 4  ·  non_pii! redacted: 2
Rollup arm(s) in the report: `[%{"arm" => "rebuild", "rollup" => "driver_load_count", "rows_affected" => 1, "source" => "aud_event"}, %{"arm" => "rebuild", "rollup" => "revenue_rollup", "rows_affected" => 0, "source" => "domain"}, %{"arm" => "rebuild", "rollup" => "product_event_rollup", "rows_affected" => 0, "source" => "domain"}]`
- PASS — KMS attests a POSITIVE :shredded tombstone
- PASS — Erasure.erased?/2 is true (tombstone + key gone + 0 active vault rows)

## 3. Run the destruction oracle CLI — `mix samen.verify.no_plaintext_pii --subject <uuid> --tiers all`

Command:
```
mix samen.verify.no_plaintext_pii --subject c0fb4a3c-1ea2-4b7e-8f8f-c6dae9839333 --tiers all \
  --replica none --domain Driftwood.Crm --domain Driftwood.Freight --domain Driftwood.Aggregate
```
Exit code: **0**

Full oracle output (the auditor artifact):
```


samen.verify.no_plaintext_pii: 17 positive post-shred attestation(s) (the erasure took — this is what you show an auditor):
  ✓ [db_content] PASS: live — no vault row for c0fb4a3c-1ea2-4b7e-8f8f-c6dae9839333 decrypts under its (destroyed) key
  ✓ [db_content] PASS: replica — no replica in this deployment (operator declared replica: :none). SEAM: a real streaming replica is an operator TODO — it inherits the same token-only ciphertext, so key-shred covers it, but wire a real scan when one exists.
  ✓ [db_content] PASS: wrong_key — no ciphertext for c0fb4a3c-1ea2-4b7e-8f8f-c6dae9839333 decrypts under any OTHER live subject key
  ✓ [db_content] PASS: rollup — every registered rollup governed by rebuild-or-exclude-on-erasure (driver_load_count:rebuild, revenue_rollup:rebuild, product_event_rollup:rebuild)
  ✓ [db_content] PASS: rollup:revenue_rollup:domain_ledger — domain-sourced rollup revenue_rollup: the erased subject has 0 surviving rows in the dpv_subscription_event ledger (dpv_customer_id) — the domain REBUILD arm's delete hook actually ran, so the recomputed rollup is subject-free by construction (content-verified, not merely report-attested).
  ✓ [db_content] PASS: rollup:product_event_rollup:domain_ledger — domain-sourced rollup product_event_rollup: the erased subject has 0 surviving rows in the fae_product_event ledger (fae_actor_ref) — the domain REBUILD arm's delete hook actually ran, so the recomputed rollup is subject-free by construction (content-verified, not merely report-attested).
  ✓ [db_content] PASS: audit — erasure recorded on the append-only aud_event tier (1 row) — the event survives (WORM), the subject is unrecoverable
  ✓ [db_content] PASS: registered_non_pii — every registered non_pii! column redacted for c0fb4a3c-1ea2-4b7e-8f8f-c6dae9839333 (the carve-out key-shred cannot reach was erased by row-level redaction)
  ✓ [backup_pitr] PASS: kms_store_backups — the external wrapped-DEK store has PITR/continuous-backup DISABLED (ADR-001 CI check 2) — the key store is excluded from the backup surface, so no restore can resurrect the shredded key
  ✓ [backup_pitr] PASS: live — the subject key is ABSENT from 'live' (no key-material column on pii_vault; no ciphertext decrypts) — a restore brings back ciphertext, never the key
  ✓ [backup_pitr] PASS: pitr_history — SEAM: no PITR-snapshot repos passed. The key-absence assertion held on the LIVE tier (the key is provably not a Postgres row there). SIMULATE the PITR history by passing `pitr_repos: [RestoredSnapshotRepo, …]` (pg_dump snapshots from the T2.5 drill). A real Neon branch-and-restore drill is an operator TODO (docs/runbooks/pitr-gameday.md).
  ✓ [kms_attestation] PASS: c0fb4a3c-1ea2-4b7e-8f8f-c6dae9839333 — KMS attests a POSITIVE :shredded tombstone (destroyed_at=~U[2026-07-17 01:32:58.083112Z], attestation_id=file-f08f1fcd967c6914) — the system of record reports the key destroyed
  ✓ [kms_attestation] PASS: c0fb4a3c-1ea2-4b7e-8f8f-c6dae9839333 — the wrapped DEK is actually GONE from the store (key_material_present? == false) — defence-in-depth over the tombstone holds
  ✓ [kms_attestation] PASS: c0fb4a3c-1ea2-4b7e-8f8f-c6dae9839333/store_backups — the attesting store has PITR/continuous-backup DISABLED (ADR-001 CI check 2)
  ✓ [trace_sink] PASS: schema — every wide-event/span field is a bounded ID / token / enum / number (J2 allow-list) — the sink only ever RECEIVED token-only data; no plaintext to shred
  ✓ [trace_sink] PASS: pseudonym — the subject's actor_id pseudonym HMAC(subject_key, c0fb4a3c-1ea2-4b7e-8f8f-c6dae9839333) is no longer computable (:shredded) — the trace-sink handle went unlinkable the moment the KMS key was destroyed (RQ5; rides oracle check 3)
  ✓ [cdc_mirror] PASS: cdc_mirror — CDC mirror not enabled in this deployment (opt-in per product, default off — doc line 635). The token-only-downstream invariant makes it safe when on; wire `config :samen_core, :cdc, adapter: …` to enable + scan it.

samen.verify.no_plaintext_pii: POST-SHRED ORACLE for subject c0fb4a3c-1ea2-4b7e-8f8f-c6dae9839333 (--tiers all)
samen.verify.no_plaintext_pii: OK — no violations found.

```
- PASS — Oracle EXITS 0 on the properly-erased driver (the auditor PASS)
- PASS — Oracle output shows positive post-shred attestations
- PASS — Oracle output shows the POSITIVE :shredded tombstone

## 4. Post-shred: the driver is UNRECOVERABLE across every tier

- PASS — The CDL is UNRECOVERABLE post-shred (reveal → {:error, :shredded})
- PASS — Vault scan: NO vault row for the driver decrypts under its (destroyed) key
- PASS — CDL plaintext absent from the driver domain row
- PASS — Driver surname absent from the driver domain row
- PASS — registered non_pii! cdl_state redacted (`[REDACTED_NON_PII]`)
- PASS — registered non_pii! cdl_expiry redacted to epoch sentinel (`1970-01-01`)

## 5. Rollup: both arms exercised + no resurrection of the driver

- PASS — REBUILD arm ran on shred (raw retained): report shows `%{"arm" => "rebuild", "rollup" => "driver_load_count", "rows_affected" => 1, "source" => "aud_event"}`
- PASS — A pre-shred rollup does NOT resurrect the driver (post-shred count = 0)
- PASS — The driver's raw dispatch events were deleted from aud_event
- PASS — SUPPRESS arm ran (window archived, simulated): report shows `%{"arm" => "suppress", "rollup" => "driver_load_count", "rows_affected" => 1, "source" => "aud_event"}`
- PASS — The suppressed driver's derived rollup rows are flagged drl_suppressed = TRUE
- PASS — Suppress-arm driver is also unrecoverable (vault scan clean)

## 6. The audit chain still VERIFIES post-shred (immutable AND crypto-shreddable)

- PASS — Org chain (`c8f24bbf-1312-44c4-a636-50bb5681bb98`) VERIFIES post-shred (1 entries, incl. the erasure event)
- PASS — Global operator chain VERIFIES post-shred (4 entries, incl. reveal lifecycle)
- PASS — The erasure event survives on the append-only aud_event tier (WORM)
- PASS — Chain entries for the driver carry NO plaintext CDL/name (tokens only)

## 7. Red paths — the oracle FAILS a decryptable copy in each tier

- PASS — RED (live tier): an un-shredded driver's vault STILL DECRYPTS → oracle violation
- PASS — RED (registered_non_pii): an un-redacted non_pii! residue → oracle violation
- PASS — RED (kms): a never-keyed subject attests :absent → oracle violation (positive tombstone required)
- PASS — RED (rollup): a registered rollup ABSENT from the erasure report → oracle violation (no resurrection allowed)
- PASS — RED (CDL cross-tier): the CDL number is unrecoverable across all tiers post-shred (no wrong-key decrypt)

## 8. Anti-tautology probe — sabotage one tier's shred, the oracle catches it, then revert

- PASS — ANTI-TAUT baseline: after a real shred the live tier is CLEAN (a :pass, not a :violation)
- PASS — ANTI-TAUT sabotage: reversing the shred (resurrected key + re-stored CDL) → oracle FLIPS to a live-tier violation
- PASS — ANTI-TAUT revert: re-shred re-seals the tier → oracle GREEN again (the flip was real, not a tautology)

## Summary

Checks: **36 PASS**, **0 FAIL**.
Oracle CLI exit code on the erased driver: **0** (0 = the auditor PASS).

**Verdict: GREEN — the crypto-shred game-day passes on the live app.**
